import React from "react";
import styled from "styled-components";

const ParticularPage = () => {
  return <Msg>상세페이지</Msg>;
};

export default ParticularPage;

const Msg = styled.div`
  color: white;
`;
