import React from "react";
import styled from "styled-components";

const Home = () => {
  return <HomeMsg>홈화면</HomeMsg>;
};

export default Home;

const HomeMsg = styled.div`
  color: white;
  margin-left: 500px;
`;
