import React from "react";
import styled from "styled-components";

const LogInPage = () => {
  return <LoginMsg>로그인 페이지</LoginMsg>;
};

export default LogInPage;

const LoginMsg = styled.div`
  color: white;
  margin-left: 500px;
`;
